# Дни без: отказ от вредных привычек

Приложение для отслеживания дней без вредных привычек. Помогает пользователям контролировать свой прогресс и мотивирует продолжать путь к здоровому образу жизни.

## 🏗 Архитектура

Проект построен на архитектуре **MVVM (Model-View-ViewModel)** с использованием **SwiftUI**.

### Почему MVVM?

- **Разделение ответственности** — логика отделена от представления
- **Тестируемость** — ViewModel легко покрывается unit-тестами
- **Реактивность** — нативная поддержка через `@Observable` / `ObservableObject`
- **Масштабируемость** — удобно добавлять новые фичи

### Схема взаимодействия

```
┌─────────────┐      ┌──────────────┐      ┌─────────────┐
│    View     │ ←──→ │  ViewModel   │ ←──→ │   Service   │
│  (SwiftUI)  │      │ (Observable) │      │   (Data)    │
└─────────────┘      └──────────────┘      └─────────────┘
                            ↓
                     ┌─────────────┐
                     │    Model    │
                     │  (Struct)   │
                     └─────────────┘
```

---

## 📁 Структура проекта

```
Дни без/
├── App/                    # Точка входа приложения
│
├── Core/                   # Базовые утилиты и расширения
│
├── Models/                 # Модели данных
│
├── ViewModels/             # ViewModel'ы (логика представления)
│
├── Services/               # Сервисы (работа с данными, API, уведомления)
│
├── UI/                     # Пользовательский интерфейс (SwiftUI Views)
│
├── Purchases/              # In-App Purchases
│
└── Assets.xcassets/        # Ресурсы (иконки, цвета, изображения)
```

### Описание папок

| Папка | Назначение |
|-------|------------|
| `App/` | Точка входа (`@main`), конфигурация приложения |
| `Core/` | Расширения типов, константы, протоколы, хелперы |
| `Models/` | Структуры данных (Habit, Category и т.д.) |
| `ViewModels/` | Классы с `@Observable` для логики представления |
| `Services/` | Бизнес-логика: хранилище, уведомления, аналитика |
| `UI/` | SwiftUI View: экраны и компоненты |
| `Purchases/` | Логика покупок и подписок (StoreKit) |

---

## 📐 Ответственность слоёв

### View (UI/)

**Что делает:**
- Отображает данные из ViewModel
- Передаёт пользовательские действия в ViewModel
- Содержит ТОЛЬКО декларативную разметку SwiftUI

**Что НЕ должно быть во View:**
- ❌ Бизнес-логика (расчёты, валидация, форматирование дат)
- ❌ Прямые вызовы сервисов
- ❌ Работа с хранилищем данных
- ❌ Сетевые запросы
- ❌ Сложные вычисления в `body`

---

### ViewModel (ViewModels/)

**Что делает:**
- Хранит состояние экрана (`@Observable` / `ObservableObject`)
- Содержит логику представления (presentation logic)
- Форматирует данные для отображения
- Вызывает сервисы и обрабатывает результаты
- Валидирует пользовательский ввод

**Что НЕ должно быть во ViewModel:**
- ❌ UI-код (SwiftUI Views, UIKit)
- ❌ Прямая работа с базой данных (только через сервисы)
- ❌ Создание других ViewModel'ов (используйте DI)

---

### Core

**Что содержит:**
- **Extensions** — расширения стандартных типов (`Date+Extensions.swift`, `String+Extensions.swift`)
- **Constants** — константы приложения (`AppConstants.swift`)
- **Protocols** — базовые протоколы (`Identifiable`, кастомные)
- **Helpers** — утилитарные функции без зависимостей
- **Modifiers** — кастомные ViewModifier'ы

**Правила Core:**
- ✅ Код должен быть переиспользуемым
- ✅ Без зависимостей от конкретных фич
- ✅ Без сайд-эффектов (чистые функции)
- ❌ Не должен импортировать Models, ViewModels, Services

---

### Models

**Что содержит:**
- Структуры данных (`struct`)
- Enum'ы для состояний и категорий
- Протоколы моделей

**Правила Models:**
- ✅ Только `struct` (value types) — не классы
- ✅ Должны быть `Codable` если сохраняются
- ✅ Должны быть `Identifiable` если используются в списках
- ✅ Должны быть `Hashable`/`Equatable` для сравнения
- ❌ Без бизнес-логики (только computed properties для удобства)

---

### Services

**Что содержит:**
- Работа с данными (CRUD операции)
- Сетевые запросы
- Push-уведомления
- Аналитика
- Интеграции с системными API

**Правила Services:**
- ✅ Всегда создавайте протокол (`HabitServiceProtocol`)
- ✅ Dependency Injection через инициализатор
- ✅ Один сервис = одна ответственность
- ❌ Без UI-зависимостей
- ❌ Без прямого доступа из View

---

## 👨‍💻 Руководство для нового разработчика

### Как добавить новый экран

1. **Создайте модель** (если нужна) в `Models/`
   ```
   Models/NewFeature.swift
   ```

2. **Создайте ViewModel** в `ViewModels/`
   ```
   ViewModels/NewFeatureViewModel.swift
   ```

3. **Создайте View** в `UI/`
   ```
   UI/Screens/NewFeatureScreen.swift
   UI/Components/NewFeatureCard.swift  // переиспользуемые компоненты
   ```

4. **Создайте/обновите Service** (если нужна работа с данными)
   ```
   Services/NewFeatureService.swift
   ```

### Структура папки UI/

```
UI/
├── Screens/           # Полноэкранные представления
│   ├── HomeScreen.swift
│   ├── AddHabitScreen.swift
│   └── SettingsScreen.swift
│
├── Components/        # Переиспользуемые компоненты
│   ├── HabitCard.swift
│   ├── ProgressRing.swift
│   └── CategoryPicker.swift
│
└── Modifiers/         # Кастомные модификаторы (или в Core/)
    └── CardStyle.swift
```

### Naming Conventions

| Тип | Паттерн | Пример |
|-----|---------|--------|
| Экран (View) | `*Screen` | `HomeScreen`, `AddHabitScreen` |
| Компонент | Описательное имя | `HabitCard`, `ProgressRing` |
| ViewModel | `*ViewModel` | `HomeViewModel`, `HabitCardViewModel` |
| Service | `*Service` | `HabitService`, `NotificationService` |
| Protocol | `*Protocol` | `HabitServiceProtocol` |
| Extension | `Type+Feature` | `Date+Formatting`, `Color+Theme` |

---

## 🚀 Запуск проекта

### Требования

- **macOS** 14.0 (Sonoma) или новее
- **Xcode** 15.0 или новее
- **iOS** 16.0+

### Установка и запуск

1. **Откройте проект в Xcode**
   ```bash
   open "Дни без.xcodeproj"
   ```

2. **Выберите симулятор** в верхней панели Xcode (например, iPhone 14)

3. **Запустите приложение** — `Cmd + R`

### Запуск на устройстве

1. Подключите iPhone к Mac
2. В Xcode: `Signing & Capabilities` → выберите команду разработчика
3. На устройстве: `Настройки → Основные → VPN и управление устройством` → доверьтесь приложению

---
